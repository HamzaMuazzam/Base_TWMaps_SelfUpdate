<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TWMaps" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TWMaps" parent="Base.Theme.TWMaps" />
    <!-- res/values/styles.xml -->
    <style name="ButtonEnabled" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_enabled_bg</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="ButtonDisabled" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_disabled_bg</item>
        <item name="android:textColor">#B0B0B0</item> <!-- Light gray text -->
    </style>

</resources>